import { combineReducers } from "redux";
import { actionDecreaseOneCounter, actionIncreaseOneCounter } from "../actions";

const rootReducer = combineReducers({
    
    });

export default rootReducer;